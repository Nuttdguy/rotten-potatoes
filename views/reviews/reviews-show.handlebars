
<div class="row">
    <div class="col-sm-6 col-offset-sm-2">

        <h3>{{review.title}}</h3>

        <small>{{review.movieTitle}}</small>
        <p>{{review.rating}}</p>
        <p>{{review.description}}</p>
        <br><a href="/">Back</a>

    </div>

</div>
